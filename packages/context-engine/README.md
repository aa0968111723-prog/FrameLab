# Context Engine

Domain module. Import from `@/lib/domain/context-engine`.

Single source of truth for project / timeline / frame / range / region / onion skin.
Serialized snapshots are what MCP and the conversation layer consume — never React state.
