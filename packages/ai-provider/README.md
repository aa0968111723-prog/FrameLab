AI provider interfaces live at `src/lib/ai/providers.ts`.

Implemented: `rife` interpolation (Practical-RIFE 4.25), `linear-blend` 快速預覽 (not AI), `pixel-metrics`, `framelab-ncc` point tracker, `block-match-16` optical flow, `rtmpose`, `locotrack`, `sea-raft`, `sam2` (click mask + forward/backward propagate), xAI Grok VLM.

Reserved (honest `MODEL_NOT_AVAILABLE` / `PROVIDER_NOT_AVAILABLE`): Video Depth Anything, Wan.

