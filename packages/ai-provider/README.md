AI provider interfaces live at `src/lib/ai/providers.ts`.

Implemented: `linear-blend` interpolation, `pixel-metrics`, `framelab-ncc` point tracker, `block-match-16` optical flow, xAI Grok VLM.

Reserved (honest `MODEL_NOT_AVAILABLE` / `PROVIDER_NOT_AVAILABLE`): SAM 2, RTMPose, SEA-RAFT, LocoTrack, Video Depth Anything, RIFE, Wan.

