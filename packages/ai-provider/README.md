AI provider interfaces live at `src/lib/ai/providers.ts`.

Implemented: `rife` interpolation (Practical-RIFE 4.25), `linear-blend` 快速預覽 (not AI), `pixel-metrics`, `framelab-ncc` point tracker, `block-match-16` optical flow, `rtmpose`, `locotrack`, `sea-raft`, xAI Grok VLM.

Reserved (honest `MODEL_NOT_AVAILABLE` / `PROVIDER_NOT_AVAILABLE`): SAM 2, Video Depth Anything, Wan.

