# GPU worker

Not built in v0.1. Future process: pull jobs (`SEGMENTATION`, `POSE_ANALYSIS`, `OPTICAL_FLOW`, `INTERPOLATION`, `GENERATIVE_REPAIR`) and write results through the same repo layer. Until then the API returns `MODEL_NOT_AVAILABLE`.
